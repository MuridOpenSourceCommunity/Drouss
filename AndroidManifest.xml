<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tab28.drouss"
    android:versionCode="2"
    android:versionName="1.0" >

    <!-- ###############################Pour Tablette seulement -->
    <!-- <supports-screens -->
    <!-- android:largeScreens="true" -->
    <!-- android:normalScreens="false" -->
    <!-- android:requiresSmallestWidthDp="600" -->
    <!-- android:smallScreens="false" -->
    <!-- android:xlargeScreens="true" /> -->
    <!-- <uses-sdk -->
    <!-- android:minSdkVersion="11" -->
    <!-- android:targetSdkVersion="14" /> -->
    <!-- ###############################FIN Pour Tablette seulement -->

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@drawable/icone"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Light.NoTitleBar" >
        <activity
            android:name="com.tab28.drouss.SpalshScreenActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.tab28.drouss.HomeActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.tab28.drouss.ThesePubListViewActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.tab28.drouss.WolofalListViewActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.tab28.drouss.TariqListViewActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.tab28.drouss.MajalisListViewActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.tab28.drouss.QassidasListViewActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.tab28.drouss.QuranListViewActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.tab28.drouss.AutreListViewActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.tab28.drouss.PdfDrouss"
            android:label="@string/app_name" >
        </activity>
    </application>

</manifest>